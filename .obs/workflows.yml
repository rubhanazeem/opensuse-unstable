workflow:
  steps:
    - link_package:
        source_project: home:Rubhan
        source_package: obs-serverx
  filters:
    architectures:
      only:
        - x86_64
    repositories:
      only:
        - openSUSE_Tumbleweed


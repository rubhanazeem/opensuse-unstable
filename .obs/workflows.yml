workflow:
  steps:
    - link_package:
        source_project: OBS:Server:Unstable
        source_package: obs-server
  filters:
    architectures:
      only:
        - x86_64
    repositories:
      only:
        - openSUSE_Tumbleweed

